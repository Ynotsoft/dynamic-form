{
  "name": "dynamic-form-workspaces",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "bun run --filter ynotsoft-dynamic-form build",
    "example": "bun --filter example dev"
  },
  "workspaces": [
    "packages/*"
  ]
}
