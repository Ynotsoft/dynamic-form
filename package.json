{
  "name": "dynamic-form-workspaces",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build-form": "bun run --filter ynotsoft-dynamic-form build",
    "build-grid": "bun run --filter ynotsoft-dynamic-grid build",
    "example": "bun --filter example dev"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "typescript": "^5.9.3"
  },
  "overrides": {
    "ajv": "^8.12.0",
    "ajv-draft-04": "^1.0.0"
  }
}
